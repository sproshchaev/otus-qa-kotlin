# Включаем параллельное выполнение
junit.jupiter.execution.parallel.enabled=true

# Методы по умолчанию в одном потоке
junit.jupiter.execution.parallel.mode.default=same_thread

# Классы по умолчанию параллельно
junit.jupiter.execution.parallel.mode.classes.default=concurrent

# Фиксированное количество потоков
junit.jupiter.execution.parallel.config.strategy=fixed
junit.jupiter.execution.parallel.config.fixed.parallelism=2

# Таймаут по умолчанию 100ms
junit.jupiter.execution.timeout.default=100ms
